bcrypt>=4.0,<5.0
Flask>=3.0,<4.0
flask-sock>=0.7,<1.0
python-dotenv>=1.0,<2.0
pyserial>=3.5,<4.0
psutil>=5.9,<7.0
RPi.GPIO>=0.7; platform_machine == "aarch64" or platform_machine == "armv7l"
# lgpio: install via 'sudo apt install python3-lgpio' then symlink into venv
# adafruit-nrfutil is used for nRF52 firmware flashing; after pip install,
# symlink it: sudo ln -sf /opt/RepeaterWatch/venv/bin/adafruit-nrfutil /usr/local/bin/adafruit-nrfutil
adafruit-nrfutil; platform_machine == "aarch64" or platform_machine == "armv7l"
adafruit-circuitpython-ina3221; platform_machine == "aarch64" or platform_machine == "armv7l"
adafruit-circuitpython-bme280; platform_machine == "aarch64" or platform_machine == "armv7l"
smbus2; platform_machine == "aarch64" or platform_machine == "armv7l"
